# Paper reading notes

Add one Markdown file per source under this folder using the [paper reading template](../templates/paper-reading.md). Follow naming and front matter rules in the [writing guide](../style.md).

When you add new files, register them under **Papers** in `mkdocs.yml` at the repository root so they appear in the site navigation (or switch to an auto-nav plugin later if the list grows large).
